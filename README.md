# ⚡️ Quick Installation

Run the following command to install the latest version:

```bash
curl -L -o install.sh "https://raw.githubusercontent.com/noelrubio143/ngixwsdnsttssl/refs/heads/main/install.sh" && chmod +x install.sh && sudo ./install.sh && rm install.sh
```
